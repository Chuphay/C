#ifndef union_find_h
#define union_find_h

#include <stdlib.h>

typedef struct data{
  int value;
  int point;
  int size;
  void *data;
} data;

data ** init(int number);

int getNode(data ** data, int a);

void myUnion(data ** data, int a, int b);

#endif
